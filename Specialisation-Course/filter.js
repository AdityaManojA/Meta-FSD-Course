const prices = [15, 80, 42, 100, 5];
const newArray=prices.filter(price=>price<50)
console.log(newArray)

const fruits = ['apple', 'mango', 'banana', 'grape', 'kiwi'];
const newArray2= fruits.filter(fruit=> fruit.includes("a"))
console.log(newArray2)


function  printLoop(array1){ //array1 is the parameter
    for(let i=0;i<array1.length;i++){
        console.log(i+1,array1[i]);
    }
}
Colors=['red','blue','green','orange','apple'];
printLoop(Colors);